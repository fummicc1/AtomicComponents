import XCTest
@testable import AtomicComponents

final class AtomicComponentsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AtomicComponents().text, "Hello, World!")
    }
}
